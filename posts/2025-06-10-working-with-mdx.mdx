export const meta = {
  title: "Working with MDX",
  summary: "Gand phatt gayi guru",
  draft: false,
  tags: ["nextjs", "mdx", "blog"],
};

# Working with MDX

MDX lets you write JSX *inside* Markdown files.  
This means you can embed components like:

```tsx
<MyCoolComponent prop="value" />
